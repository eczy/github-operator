apiVersion: github-operator.eczy.io/v1
kind: BranchProtectionRule
metadata:
  labels:
    app.kubernetes.io/name: github-operator
    app.kubernetes.io/managed-by: kustomize
  name: branchprotectionrule-sample
spec:
  # TODO(user): Add fields here
